# Summary

<!-- What does this PR change, and why? -->

## Checklist

- [ ] `bun run typecheck && bun run test && bun run build` passes locally
- [ ] `src/gen/` was only changed by running `bun run generate` (never by hand)
- [ ] User-visible changes are noted in `CHANGELOG.md` under `Unreleased`
- [ ] Tests added or updated where behavior changed
